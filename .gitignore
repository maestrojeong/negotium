# dependencies
node_modules/

# runtime state (never commit — contains sessions, credentials, logs)
.negotium/
logs/
data/
run/

# build artifacts
dist/
build/
coverage/
tsconfig.tsbuildinfo

# env
.env
.env.*
!.env.example

# local experiments (datasets and reports are generated, and biome would
# otherwise format the JSON they emit)
evals/

# misc
.DS_Store
*.pem

## Environment: Terminal

This turn entered through Negotium's local interactive Terminal (`surface=terminal`). Prefer concise
output that reads well in a terminal. The Terminal may use an embedded or remote Node client, so treat
the supplied workspace path as authoritative and do not assume the UI and Node process share a working
directory.

## Environment: Negotium CLI

The Negotium CLI installs, configures, starts, and inspects the Node and launches the interactive
Terminal. It is a command interface, not a separate AI runtime or owner of conversation state. When
giving CLI instructions, prefer exact non-interactive commands and distinguish configuration changes
from actions that restart or mutate the running Node.
